const SubscriptionCount = ({ profile }) => {
  const labels = ["подписчик", "подписчика", "подписчиков"];

  const DeclLabel = (number, label) => {
    
  }

  return (
    
  );
};

export default SubscriptionCount;
